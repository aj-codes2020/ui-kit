import { Canvas, Meta } from '@storybook/blocks';
import * as PaginationStories from './Pagination.stories';

<Meta of={PaginationStories} />

# Pagination

The `Pagination` component allows you to manage navigation through pages of content, providing controls for moving between pages and indicating the current page.

<Canvas of={PaginationStories.Default} />

## Usage

To use the `Pagination` component, import it and pass the necessary props:

```tsx
import { Pagination } from '@aj-codes2020/ui-kit';

const handlePageChange = (page: number) => {
  console.log(`Current page is ${page}`);
};

<Pagination
  totalItems={50}
  itemsPerPage={10}
  currentPage={1}
  onPageChange={handlePageChange}
  className=""
/>
```

## Props

The `Pagination` component accepts the following props:

<div style={{ overflowX: 'auto' }}>
  <table style={{ width: '100%' }}>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Description</th>
        <th>Required</th>
        <th>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`totalItems`</td>
        <td>number</td>
        <td>The total number of items to paginate.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`itemsPerPage`</td>
        <td>number</td>
        <td>The number of items to display per page.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`currentPage`</td>
        <td>number</td>
        <td>The current active page.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`onPageChange`</td>
        <td>(page: number) => void</td>
        <td>Callback function that is called when the page changes.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`className`</td>
        <td>string</td>
        <td>Additional CSS classes to apply to the pagination container.</td>
        <td>No</td>
        <td>''</td>
      </tr>
    </tbody>
  </table>
</div>

## Class Selectors

You can customize the `Pagination` component using CSS. The following class names are available for customization:

`pagination-container`: The main container for the pagination component.\
`pagination-button`: The button element for each page and navigation control.\
`pagination-button.active`: The button element for the active page.\
`pagination-button:disabled`: The button element when navigation is disabled.
