import { Canvas, Meta } from '@storybook/blocks';
import * as CardStories from './Card.stories';

<Meta of={CardStories} />

# Card

The `Card` component is a versatile container that can include a header, body, and footer. It is useful for displaying content in a structured and visually appealing manner.

<Canvas of={CardStories.Default} />
<Canvas of={CardStories.WithoutHeader} />
<Canvas of={CardStories.WithoutFooter} />

## Usage

To use the `Card` component, import it and pass the necessary props:

```tsx
import { Card } from '@aj-codes2020/ui-kit';

<Card
  header="Card Header"
  footer="Card Footer"
  className=""
>
  This is the body of the card.
</Card>
```

## Props

The `Card` component accepts the following props:

<div style={{ overflowX: 'auto' }}>
  <table style={{ width: '100%' }}>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Description</th>
        <th>Required</th>
        <th>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`header`</td>
        <td>React.ReactNode</td>
        <td>The content to display in the header of the card.</td>
        <td>No</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`footer`</td>
        <td>React.ReactNode</td>
        <td>The content to display in the footer of the card.</td>
        <td>No</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`children`</td>
        <td>React.ReactNode</td>
        <td>The content to display in the body of the card.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`className`</td>
        <td>string</td>
        <td>Additional CSS classes to apply to the card container.</td>
        <td>No</td>
        <td>''</td>
      </tr>
    </tbody>
  </table>
</div>

## Class Selectors

You can customize the `Card` component using CSS. The following class names are available for customization:

`card`: The main container for the card.\
`card-header`: The container for the card's header.\
`card-body`: The container for the card's body.\
`card-footer`: The container for the card's footer.
