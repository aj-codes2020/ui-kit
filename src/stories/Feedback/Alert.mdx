import { Canvas, Meta } from '@storybook/blocks';
import * as AlertStories from './Alert.stories';

<Meta of={AlertStories} />

# Alert

The Alert component is used to display important messages to the user, such as informational, success, warning, and error alerts.

<Canvas of={AlertStories.Informational} />
<Canvas of={AlertStories.Success} />
<Canvas of={AlertStories.Warning} />
<Canvas of={AlertStories.Error} />


## Usage

To use the Alert component, import it and pass the `type` and `message` properties.

```tsx
import { Alert } from 'path-to-your-components';

<Alert type="info" message="This is an informational alert" />;
<Alert type="success" message="This is a success alert" />;
<Alert type="warning" message="This is a warning alert" />;
<Alert type="error" message="This is an error alert" />;
```

## Props 

The `Alert` component accepts the following props:

### type

**Type**: `'info' | 'success' | 'warning' | 'error'`\
**Description**: Specifies the type of alert to display. Each type corresponds to a different style and purpose.\
**Required**: Yes

### message

**Type**: `string`\
**Description**: The message to display within the alert.\
**Required**: Yes

### className

**Type**: `string`\
**Description**: Additional CSS classes to apply to the alert container. Use this prop to further customize the alert's appearance.\
**Required**: No\
**Default**: `''`

## Customization

You can customize the Alert component using CSS. The following class names are available for customization:

`alert`: The main container for the alert.\
`alert-info`: The container for an informational alert.\
`alert-success`: The container for a success alert.\
`alert-warning`: The container for a warning alert.\
`alert-error`: The container for an error alert.

