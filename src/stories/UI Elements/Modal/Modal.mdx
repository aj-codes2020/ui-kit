import { Canvas, Meta } from '@storybook/blocks';
import * as ModalStories from './Modal.stories';

<Meta of={ModalStories} />

# Modal

The `Modal` component provides a way to create modal dialogs for user interactions such as forms, notifications, or confirmations.

<Canvas of={ModalStories.Default} />

## Usage

To use the `Modal` component, import it and use it directly in your component:

```tsx
import { Modal } from '@aj-codes2020/ui-kit';

const App = () => {
  return (
    <Modal
      title="Modal Title"
      trigger={<button>Open Modal</button>}
    >
      <p>This is the content of the modal.</p>
    </Modal>
  );
};
```

## Props

The `Modal` component accepts the following props:

<div style={{ overflowX: 'auto' }}>
  <table style={{ width: '100%' }}>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Description</th>
        <th>Required</th>
        <th>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`title`</td>
        <td>string</td>
        <td>The title of the modal.</td>
        <td>No</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`children`</td>
        <td>ReactNode</td>
        <td>The content to display inside the modal.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`trigger`</td>
        <td>ReactNode</td>
        <td>The element that triggers the modal when clicked.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`className`</td>
        <td>string</td>
        <td>Additional CSS classes to apply to the modal.</td>
        <td>No</td>
        <td>''</td>
      </tr>
    </tbody>
  </table>
</div>

## Class Selectors

You can customize the `Modal` component using CSS. The following class names are available for customization:

`modal-overlay`: The overlay behind the modal.\
`modal-overlay.open`: The class to apply when the modal is open.\
`modal`: The main container for the modal content.\
`modal-header`: The header of the modal, containing the title.\
`modal-body`: The body of the modal, containing the main content.\
`modal-close`: The button to close the modal.
