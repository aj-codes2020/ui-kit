import { Canvas, Meta } from '@storybook/blocks';
import * as SpinnerStories from './Spinner.stories';

<Meta of={SpinnerStories} />

# Spinner

The `Spinner` component is used to indicate loading or processing states in your application.

<Canvas of={SpinnerStories.Default} />

## Usage

To use the `Spinner` component, import it and pass the necessary props:

```tsx
import { Spinner } from '@aj-codes2020/ui-kit';

const App = () => {
  return (
    <Spinner size="medium" color="primary" />
  );
};
```



## Props

The `Spinner` component accepts the following props:

<div style={{ overflowX: 'auto' }}>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Description</th>
        <th>Required</th>
        <th>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`size`</td>
        <td>'small' | 'medium' | 'large'</td>
        <td>The size of the spinner.</td>
        <td>No</td>
        <td>'medium'</td>
      </tr>
      <tr>
        <td>`color`</td>
        <td>'primary' | 'secondary' | 'success' | 'warning' | 'danger'</td>
        <td>The color of the spinner.</td>
        <td>No</td>
        <td>'primary'</td>
      </tr>
      <tr>
        <td>`className`</td>
        <td>string</td>
        <td>Additional CSS classes to apply to the spinner component.</td>
        <td>No</td>
        <td>''</td>
      </tr>
    </tbody>
  </table>
</div>

## Class Selectors

You can customize the `Spinner` component using CSS. The following class names are available for customization:

`spinner`: The main container for the spinner component.\
`spinner-circle`: The animated circle of the spinner.\
`spinner.small .spinner-circle`: Styles for the small spinner.\
`spinner.medium .spinner-circle`: Styles for the medium spinner.\
`spinner.large .spinner-circle`: Styles for the large spinner.\
`spinner.primary .spinner-circle`: Styles for the primary color.\
`spinner.secondary .spinner-circle`: Styles for the secondary color.\
`spinner.success .spinner-circle`: Styles for the success color.\
`spinner.warning .spinner-circle`: Styles for the warning color.\
`spinner.danger .spinner-circle`: Styles for the danger color.
