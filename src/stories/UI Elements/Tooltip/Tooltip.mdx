import { Canvas, Meta } from '@storybook/blocks';
import * as TooltipStories from './Tooltip.stories';

<Meta of={TooltipStories} />

# Tooltip

The `Tooltip` component provides additional information when users hover over or focus on an element, typically with a short description or hint.

<Canvas of={TooltipStories.Default} />

## Usage

To use the `Tooltip` component, import it and wrap it around the element you want to have a tooltip:

```tsx
import { Tooltip, Button } from '@aj-codes2020/ui-kit';

const App = () => {
  return (
    <Tooltip content="This is a tooltip" position="top">
      <Button label="Hover Over Me" onClick={() => {}} primary />
    </Tooltip>
  );
};
```

## Props

The `Tooltip` component accepts the following props:

<div style={{ overflowX: 'auto' }}>
  <table style={{ width: '100%' }}>
    <thead>
      <tr>
        <th>Name</th>
        <th>Type</th>
        <th>Description</th>
        <th>Required</th>
        <th>Default</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td>`content`</td>
        <td>string</td>
        <td>The content to display inside the tooltip.</td>
        <td>Yes</td>
        <td>N/A</td>
      </tr>
      <tr>
        <td>`position`</td>
        <td>'top' | 'bottom' | 'left' | 'right'</td>
        <td>The position of the tooltip relative to the wrapped element.</td>
        <td>No</td>
        <td>'top'</td>
      </tr>
      <tr>
        <td>`className`</td>
        <td>string</td>
        <td>Additional CSS classes to apply to the tooltip container.</td>
        <td>No</td>
        <td>''</td>
      </tr>
    </tbody>
  </table>
</div>

## Class Selectors

You can customize the `Tooltip` component using CSS. The following class names are available for customization:

`tooltip-container`: The main container for the tooltip and the element it wraps.\
`tooltip-box`: The tooltip box that contains the content.\
`tooltip-top`: Tooltip box when positioned at the top.\
`tooltip-bottom`: Tooltip box when positioned at the bottom.\
`tooltip-left`: Tooltip box when positioned at the left.\
`tooltip-right`: Tooltip box when positioned at the right.
